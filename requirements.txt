# Atlas execution platform — pinned dependencies
# Install: pip install --break-system-packages -r requirements.txt
# numpy and scipy-free since the research stack moved to Crucible.

# Data
numpy==2.4.2
pandas==3.0.1
pyarrow>=16.0          # parquet cache reads (dashboard builder)

# Brokers
alpaca-py==0.43.2
ib_insync>=0.9.86      # IB micro-futures adapter (BOREAS, ~2026-08-28)

# Dashboard backend
fastapi>=0.110
uvicorn>=0.29
websockets>=12.0

# Utilities
requests==2.32.5
pandas_market_calendars>=4.0   # NYSE session calendar (kernel.market_hours)
