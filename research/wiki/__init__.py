"""Atlas wiki materializer -- SQL knowledge layer → reviewable markdown.

Phase 7 of the research-system consolidation.  The SQL tables built in
Phases 0-6 are the source of truth; this package projects them into
markdown files that operators can diff in PRs and (optionally, in the
future) feed into an external wiki agent for prose synthesis.
"""
