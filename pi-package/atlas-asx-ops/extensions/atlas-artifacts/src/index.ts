import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function atlasArtifactsExtension(_pi: ExtensionAPI) {
  // Placeholder for:
  // - atlas_artifacts_load(path)
  // - atlas_artifacts_summarize(kind, path)
  // - atlas_artifacts_compare(left, right)
}
