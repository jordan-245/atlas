import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function atlasStateExtension(_pi: ExtensionAPI) {
  // Placeholder for persistent run registry, locks, and correlation IDs.
}
