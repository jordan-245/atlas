import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export default function atlasRiskGatesExtension(_pi: ExtensionAPI) {
  // Placeholder for:
  // - promotion gate checks for new config candidates
  // - paper trade approval confirmations
  // - write protection around active_config.json and plan execution
}
